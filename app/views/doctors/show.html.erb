<%= @doctor.name %>
<%= @doctor.department %>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.appointment_time %></li>
    <li><%= link_to appointment.patient_name, patient_path(appointment.patient) %></li><br>
  <% end %>
</ul>
